// Red "LIVE" pill — overlay on hero clip and any future fresh-cluster tile.
// Shape + color mirror the sport-channel reference design.
export function LivePill() {
  return (
    <span className="inline-flex items-center bg-gradient-to-r from-coral-light to-coral text-white text-[11px] font-bold uppercase tracking-[0.08em] px-3 py-1 rounded-full">
      Live
    </span>
  );
}
